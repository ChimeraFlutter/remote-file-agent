name: remote_file_agent
description: A Flutter package for remote file management agent with WebSocket communication, automatic reconnection, and file operations.
version: 1.0.0
homepage: https://github.com/chimera/remote_file_agent

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  uuid: ^4.0.0
  path: ^1.8.3
  archive: ^3.4.10
  crypto: ^3.0.3
  shared_preferences: ^2.2.2
  logger: ^2.0.2

dev_dependencies:
  flutter_lints: ^6.0.0
